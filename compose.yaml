services:
  app:
    build:
      context: .
      dockerfile: docker/develop/Dockerfile
    volumes:
      - ./:/workspace
      - target:/workspace/target
      - cargo:/usr/local/cargo/registry

volumes:
  target:
  cargo:
